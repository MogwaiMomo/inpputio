extends layout

block content
	.jumbotron
		if (!user)
			h1= app_uvp
			h2.subhead Inpputio uses NLP to quickly pull out and organize the most common and emotionally charged messages in customer reviews and open-form survey responses.   	
			a(href="signup")
				button.btn.btn-danger(type='button') Sign Up (It's FREE)
		if (user)
			h3 Welcome to Inpput!
			p You are currently logged in as #{user.username}.
			a(href="/users/#{user.username}")
				button.btn.btn-info(type='button') Go to your dashboard
			
		